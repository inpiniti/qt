# QueryClientProvider

Component to provide the `QueryClient` to your application.

```jsx
<QueryClientProvider client={queryClient}>
  <App />
</QueryClientProvider>
```

## useQueryClient
Hook to return the current `QueryClient` instance.

```jsx
const queryClient = useQueryClient()
```
