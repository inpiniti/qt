# useQuery

```jsx
const {
  data,
  error,
  status,
  fetchStatus,
  isLoading,
  isPending,
  isSuccess,
  isError,
  isFetching,
  refetch,
} = useQuery({
  queryKey,
  queryFn,
  enabled,
  staleTime,
  gcTime,
  refetchOnWindowFocus,
  retry,
  select,
  placeholderData,
})
```
