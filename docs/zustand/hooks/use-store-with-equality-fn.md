
# useStoreWithEqualityFn

`createWithEqualityFn`으로 생성된 스토어를 사용할 때 기본적으로 사용되는 훅입니다.

## 사용법
직접 import해서 사용하는 경우는 드물며, `createWithEqualityFn`의 반환값이 이 훅입니다.

```javascript
import { useStoreWithEqualityFn } from 'zustand/traditional'
```
