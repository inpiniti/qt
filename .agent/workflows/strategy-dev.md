---
description: 새로운 매매 전략 개발 워크플로우
---

# 📈 매매 전략 개발 워크플로우

이 워크플로우는 새로운 매매 전략이나 보조지표를 프로젝트에 추가하는 절차입니다.

## 사용 시점

- 새로운 보조지표 추가 시
- 새로운 매매 필터 추가 시
- 기존 전략 로직 수정 시

## 필수 참조

- `.agent/skills/trading-logic.md`: 기존 지표 계산 공식
- `src/lib/dataProcessor.js`: 구현 코드
- `README.md`: 전략 명세

## 워크플로우 단계

### 1. 전략 설계

```markdown
## 전략 설계 템플릿

### 전략 이름
[예: RSI 다이버전스 필터]

### 목적
[왜 이 전략이 필요한가?]

### 계산 공식
[수학적 공식 또는 조건문]

### 매수 조건
[이 전략이 True일 때 매수 조건]

### 매도 조건
[이 전략이 True일 때 매도 조건]

### 예상 효과
[승률/수익률 향상 예상치]
```

### 2. 지표 계산 로직 구현

**파일**: `src/lib/dataProcessor.js`

```javascript
// 예시: 새로운 지표 계산 함수
function calculateNewIndicator(data, period = 14) {
  return data.map((item, idx) => {
    // 지표 계산 로직
    const value = /* 계산식 */;
    return {
      ...item,
      newIndicator: value
    };
  });
}
```

### 3. 시뮬레이션 로직 통합

**파일**: `src/lib/dataProcessor.js` (`simulateTrade` 함수)

```javascript
// 새 필터 조건 추가
if (strategyOptions.useNewFilter) {
  if (!checkNewFilterCondition(item)) {
    continue; // 조건 불충족 시 패스
  }
}
```

### 4. UI 설정 추가

**파일**: `src/components/layout/Sidebar.jsx`

```jsx
// 새 전략 옵션 체크박스 추가
<div className="flex items-center gap-2">
  <Checkbox
    id="newFilter"
    checked={strategyOptions.useNewFilter}
    onCheckedChange={(checked) => 
      setStrategyOptions({ ...strategyOptions, useNewFilter: checked })
    }
  />
  <label htmlFor="newFilter" className="text-[#cccccc]">
    새 필터
  </label>
</div>
```

### 5. Store 상태 추가

**파일**: `src/store/useStore.js`

```javascript
strategyOptions: {
  // 기존 옵션들...
  useNewFilter: false,  // 새 필터 옵션 추가
},
```

### 6. 자동 매매 로직 반영

**파일**: `src/lib/autoTradeLogic.js`

- 자동 매매 설정에도 새 전략 옵션 반영

### 7. 백테스팅 검증

// turbo
```powershell
npm run dev
```

**검증 항목**:
- [ ] 시뮬레이션 결과가 예상과 일치하는가?
- [ ] 최소 1년 데이터로 테스트했는가?
- [ ] 수익률/승률이 개선되었는가?
- [ ] 거래 횟수가 극단적으로 변하지 않았는가?

### 8. 문서 업데이트

**파일**: `README.md`

- 매매 전략 섹션에 새 필터 추가
- 조건 설명 및 효과 기술

**파일**: `.agent/skills/trading-logic.md`

- 새 지표 계산 공식 추가

## 전략 개발 체크리스트

### 필수
- [ ] `dataProcessor.js`에 지표 계산 함수 추가
- [ ] `simulateTrade`에 필터 조건 추가
- [ ] `Sidebar.jsx`에 UI 옵션 추가
- [ ] `useStore.js`에 상태 추가
- [ ] 백테스팅 완료

### 선택
- [ ] `autoTradeLogic.js`에 반영
- [ ] `README.md` 문서 업데이트
- [ ] `trading-logic.md` 스킬 업데이트

## 전략 검증 기준

| 지표 | 기준 |
|------|------|
| 승률 | >= 50% |
| 수익률 | > 0% (연 기준) |
| 최대 낙폭 | < 20% |
| 거래 횟수 | 적정 (너무 적거나 많지 않음) |

## 주의사항

1. **과적합 경계**: 과도한 필터는 백테스팅 결과만 좋고 실전에서 효과 없음
2. **비용 반영**: 수수료(0.05%)와 슬리피지(0.1%) 항상 고려
3. **시장 구분**: 상승장/하락장 모두에서 테스트
4. **단순함 유지**: 복잡한 전략보다 단순한 전략이 더 견고함
